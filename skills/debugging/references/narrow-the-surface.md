# Narrow The Surface

Shrink the search space before fixing:

- isolate the failing layer
- remove unrelated variables
- test one seam at a time
- prefer the smallest reproduction that still fails

If you still cannot localize the issue, gather more evidence instead of changing code.
