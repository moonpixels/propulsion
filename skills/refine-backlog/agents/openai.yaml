interface:
    display_name: 'Refine Backlog'
    short_description: 'Keep backlog work current and honestly ready'
    default_prompt: 'Use $refine-backlog to refine this bounded backlog into verified current, prioritised work.'
policy:
    allow_implicit_invocation: false
